    <div class="modal-dialog">
    <div class="modal-content">
    <div class="modal-header">
            <h3 class="modal-title"><a href="mailto:{{I.Student.Email}}">{{fullname I.Student}}</a></h3>
    </div>    
        <div class="modal-body form-horizontal">
            <dl class="dl-horizontal">
                <dt>promotion</dt>
                <dd>{{I.Promotion}}</dd>
                <dt>major</dt>
                <dd>
                    <select onchange="updateMajor(this,'{{I.Student.Email}}')">
                        {{majorOptions I.Major}}
                    </select>
                </dd>                
                <dt>academic tutor</dt>
                <dd>
                    <a href="mailto:{{I.Tutor.Email}}">{{fullname I.Tutor}}</a>
                </dd>


            </dl>

            <h4 class="text-center">{{company I.Cpy}}</h4>
            <dl class="dl-horizontal">
                <dt>period</dt>
                <dd>{{date I.Begin}} to {{date I.End}}</dd>
                <dt>supervisor</dt>
                <dd>
                    <a href="mailto:{{I.Sup.Email}}">{{fullname I.Sup}}</a>
                    ({{I.Sup.Tel}})
                </dd>
            </dl>

                    <!--<h6>Supervisor evaluation</h6>

                    <dl class="dl-horizontal">
                        <dt>due date</dt>
                        <dd><input type="text" data-provide="datepicker" class="form-control date input-sm" value="{{shortDate SupReport.Deadline}}"></dd>
                        <dt>grade</dt>
                        <dd><input type="number" class="form-control input-sm" value="{{SupReport.Grade}}" onchange="setMark('{{Stu.P.Email}}', 'supReport','SupReport', this)"></dd>
                    </dl>

                    <a href="/api/v1/conventions/{{Stu.P.Email}}/supervisor/report" id='dl-supervisor' class='btn btn-wide btn-success'>
                        <i class='glyphicon glyphicon-cloud-download'></i> Get the report
                    </a>
                    <input type="file" id="up-supervisor" name="up-supervisor" style="display: none;">-->
        </div>
    </div>
    </div>